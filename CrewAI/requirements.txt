# Core dependencies
crewai[tools]>=0.100.1,<1.0.0
pydantic>=2.0.0,<3.0.0
python-dotenv>=1.0.0

# Development dependencies (optional)
pytest>=7.0.0
pytest-asyncio>=0.21.0
black>=23.0.0
isort>=5.12.0
mypy>=1.0.0

# Calendar integration (custom module)
# Note: my_calendar_module needs to be installed separately
# my_calendar_module 